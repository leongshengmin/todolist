<%= form_for @tasklist, url: user_tasklist_index_path(params[:user_id]) do |f| %>
	<%= f.label :title, class: "label" %>
	<%= f.text_field :title, class: 'form-control' %>
<%= f.submit "Add", class: "button  show-btn" %>
<% end %>
