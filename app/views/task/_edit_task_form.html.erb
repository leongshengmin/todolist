<%= form_for @task, url: user_task_path(params[:user_id], @task.id) do |f| %>
	<%= f.label :title %>
	<%= f.text_field :title, class: 'form-control' %>

	<%= f.label :date %>
	<%= f.date_select :date, :order => [:day, :month, :year] %>
	
	<%= f.submit "Amend", class: "btn btn-primary"%>
<% end %>